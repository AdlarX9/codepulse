# Certificates

This directory contains the certificates used by the nginx server.

They must be named `fullchain.pem` and `privkey.pem`, those names are also given to the certificate files of Let's Encrypt.
